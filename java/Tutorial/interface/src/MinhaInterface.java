//Em programação orientada a objetos, uma interface é uma coleção de métodos abstratos
// (sem implementação) que uma classe pode implementar. Ela define um contrato para
// as classes que a implementam, garantindo que essas classes forneçam implementações
// para os métodos declarados na interface.
public interface MinhaInterface {
    void metodo1();
    int metodo2(String parametro);
}
